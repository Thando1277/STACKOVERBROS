// config.js - Local configuration (not committed to git)
// Each developer uses their own IP address

const Config = {
  // ⚠️ CHANGE THIS TO YOUR LOCAL IP ADDRESS
  // Find your IP:
  //   Windows: ipconfig (look for IPv4 Address)
  //   Mac/Linux: ifconfig | grep "inet "
  API_URL: 'http://10.250.47.102:5000',  // 👈 Each person changes this
};

export default Config;
